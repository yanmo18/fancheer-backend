---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 1822362781095152_0/project_7665287858529468722-files/PRD/听潮阁粉丝官网-PRD-V2.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 1822362781095152#1784792401403
    ReservedCode2: ""
---
# 听潮阁虚拟主播粉丝官网 V1.0 完整版PRD（V2.0 修订版）

> 文档版本：V2.1（技术选型更新：纯 CSS + Redis + Navicat + 开发模式）
> 更新日期：2026-07-23
> 修订说明：补充点赞防抖、角色三级区分、私密消息权限收紧、性能原则、Open Questions 全部已解答、部署方案、成本方案；V2.1 更新技术选型（Tailwind→纯CSS、新增 Redis/Navicat/Redis Insight、明确开发模式）

---

## 0 文档总述

项目名称：听潮阁主播粉丝专属官网 V1.0 MVP
产品属性：个人开发、非盈利粉丝展示 + 轻量化互动站点
技术开发规则：核心业务代码手写，AI 仅生成样式、CRUD 模板代码；沉淀整套交付流程用于后续接单复用
禁止功能：集资、榜单、氪金、打投、粉丝上传资源、飘屏、消息推送、AI 工具、文件打包下载、**粉丝私信互聊、数据导出**
全站路由规划：`/`首页、`/chat`聊天室、`/user`个人中心、`/login`登录页、`/register`注册页、`/admin`管理后台

### 核心设计原则

1. **极致性价比**：个人开发者项目，全流程控制成本，能用免费方案不用付费方案，能用轻量方案不用重量级方案
2. **性能优先、拒绝卡顿**：所有页面必须做到首屏快速加载，静态资源压缩+懒加载+按需请求，严禁因加载内容过多导致页面卡顿
3. **内存节约**：接口返回最小化数据量，前端避免大数据缓存，图片/音频全部压缩后传输
4. **安全兜底**：所有用户输入后端强制校验，前端仅做视觉辅助提示

---

## 1 问题陈述（Problem Statement）

### 用户痛点

听潮阁虚拟主播的粉丝群体目前分散在微博、B站动态、QQ群等多个平台获取主播信息和互动，存在以下核心问题：

1. **信息分散，缺乏统一入口**：主播个人资料、获奖记录、图集、活动日历、音乐作品等核心内容散落在不同平台，粉丝需要反复切换才能获取完整信息，体验碎片化。
2. **缺乏专属粉丝互动空间**：现有平台（B站评论区、微博留言）是公开广场式互动，粉丝无法向主播发送私密消息表达支持，也没有一个"只属于这个圈子"的归属感和社区氛围。
3. **主播-粉丝关系缺乏可视化呈现**：主播所在公会的人物关系、群像故事等内容只存在于直播口述或粉丝自发整理中，没有一个正式的、可视化载体来呈现这个"世界观"。

### 受影响人群

- **核心粉丝群体**（高频用户）：每天关注主播动态、希望与主播直接互动的忠实粉丝
- **路人粉丝**（低频用户）：偶然接触到主播内容，想了解基本信息的新粉
- **主播本人/管理员**：需要一个轻量后台管理站点内容和维护粉丝互动

### 不解决的代价

- 粉丝持续流失到竞品主播的粉丝社区（如果有类似站点）
- 主播的个人品牌价值无法在一个专属阵地沉淀
- 粉丝互动停留在平台层面，缺乏深度连接

---

## 2 目标（Goals）

| # | 目标 | 衡量方式 | 目标值 |
|---|------|---------|--------|
| G1 | 成为听潮阁粉丝群体的**信息中枢** | 首页各模块内容覆盖率 | ≥ 90% 核心信息在一站内可获取 |
| G2 | 提供**轻量但有效的粉丝互动**能力 | 聊天室日均消息量、打卡参与率 | 上线首月日均消息 ≥ 20 条，打卡率 ≥ 30% |
| G3 | 打造**群像世界观可视化**的独特体验 | 图谱页面访问量占比 | 图谱页面 UV 占全站 UV ≥ 40% |
| G4 | 完成**可复用的个人项目交付模板** | 开发流程 SOP 完整度 | 沉淀完整 SOP 文档，后续接单可直接复用 |
| G5 | 保证站点**稳定运行与极致性能** | 首屏加载时间、接口响应时间 | 首屏加载 < 2s，P95 接口响应 < 500ms |
| G6 | **控制运营成本** | 月均服务器+域名费用 | 月均 ≤ 100 元（初期） |

---

## 3 非目标（Non-Goals）

| 非目标 | 排除原因 |
|--------|---------|
| 飘屏动画 | 性能开销大、影响阅读体验，与"安静展示+轻互动"定位不符 |
| 短信/邮件消息推送 | 非盈利项目无预算，轮询已满足实时性需求 |
| 图集批量打包下载 | 图片为主播个人版权，避免被盗批量下载；减少带宽压力 |
| AI 文案/海报工具 | 超出粉丝官网定位，增加开发复杂度和维护成本 |
| 粉丝自主上传音视频图片 | 版权和内容审核风险，内容统一由管理员后台上传 |
| 多级评论回复 | 聊天室定位为轻量留言墙，多级评论大幅增加复杂度和管理成本 |
| 独立提问页面 | 私密消息已能实现粉丝向主播提问需求 |
| WebSocket 实时通信 | V1.0 用户量级（< 500 同时在线）轮询完全够用，WebSocket 增加服务端复杂度 |
| 打投/榜单/氪金功能 | 违背非盈利定位 |
| **粉丝私信互聊** | 产品定位为"粉丝→主播"单向私密互动，不做粉丝间私信，降低开发复杂度和内容管理风险 |
| **数据导出功能** | 个人开发者项目初期不需要，数据库可直接备份 |

---

## 4 角色与权限矩阵（三级角色区分）

| 角色 | 标识 | 访问权限 | 操作权限 |
|------|------|---------|---------|
| **游客** | 未登录 | 仅访问首页所有展示内容 | 无法打卡、发消息、点赞、进入聊天室与个人中心 |
| **注册粉丝** | `fan` | 首页、聊天室、个人中心 | ① 个人中心：选用系统头像、修改展示昵称、每日打卡、查看打卡日历 ② 聊天室：发送公开/私密消息、点赞公开消息、提交举报 ③ 仅可见本人私密消息、**主播对自己的私密回复** |
| **管理员** | `admin` | 全部前台页面 + 独立管理后台 | 拥有粉丝全部权限；后台管理全站素材、处置举报、删除违规消息、封禁违规账号、维护关系图谱数据；**不可查看粉丝私密消息** |
| **主播** | `streamer` | 全部前台页面 + 独立管理后台 | 拥有管理员全部权限；**聊天室可查看并回复所有私密消息**；**私密消息仅主播可见和回复，管理员无权读取** |

### 关键权限区分说明

- **主播（streamer）vs 管理员（admin）**：主播是唯一可以查看和回复粉丝私密消息的角色；管理员负责站点内容管理和举报处理，但不接触粉丝私密消息
- **私密消息可见范围**：发送人本人 + 主播（streamer），管理员（admin）无权读取
- **建表要求**：用户表 `role` 字段必须有 `fan` / `admin` / `streamer` 三个枚举值，严格区分

### 注册规则

- 登录账号永久不可修改
- 密码采用 bcrypt 加盐加密存储
- 搭配 `svg-captcha` 图形验证码抵御批量注册（纯后端生成 SVG，零成本，不接入第三方服务）
- 不接入手机、邮箱验证
- 注册时需勾选同意《用户协议》

---

## 5 用户故事（User Stories）

### 5.1 游客视角

| # | 用户故事 | 优先级 |
|---|---------|--------|
| US-01 | 作为一个**路人粉丝**，我希望**在首页看到主播的完整资料、获奖记录和图集**，以便快速了解主播 | P0 |
| US-02 | 作为一个**路人粉丝**，我希望**通过群像关系图谱了解主播所在公会的人物关系** | P0 |
| US-03 | 作为一个**路人粉丝**，我希望**在首页听到主播的音乐作品** | P0 |
| US-04 | 作为一个**路人粉丝**，我希望**看到近期活动日历** | P1 |

### 5.2 注册粉丝视角

| # | 用户故事 | 优先级 |
|---|---------|--------|
| US-05 | 作为一个**粉丝**，我希望**在聊天室发送公开消息**，与其他粉丝互动 | P0 |
| US-06 | 作为一个**粉丝**，我希望**给主播发送私密消息**，说不想公开说的话 | P0 |
| US-07 | 作为一个**粉丝**，我希望**每天打卡签到**，记录对主播的持续陪伴 | P0 |
| US-08 | 作为一个**粉丝**，我希望**查看自己的打卡日历** | P0 |
| US-09 | 作为一个**粉丝**，我希望**修改展示昵称和头像** | P0 |
| US-10 | 作为一个**粉丝**，我希望**点赞喜欢的公开消息**（点赞后不能连点，需要等一会才能再操作） | P1 |
| US-11 | 作为一个**粉丝**，我希望**举报违规消息** | P1 |
| US-12 | 作为一个**粉丝**，我希望**看到主播对我的私密回复** | P0 |

### 5.3 主播视角（streamer）

| # | 用户故事 | 优先级 |
|---|---------|--------|
| US-13 | 作为**主播**，我希望**查看所有粉丝发给我的私密消息并回复** | P0 |
| US-14 | 作为**主播**，我希望**在聊天室发送公开回复和私密回复** | P0 |
| US-15 | 作为**主播**，我希望**拥有管理员的所有后台管理权限** | P0 |

### 5.4 管理员视角（admin）

| # | 用户故事 | 优先级 |
|---|---------|--------|
| US-16 | 作为**管理员**，我希望**管理后台所有展示内容**（Banner、资料、图集、音乐、活动、图谱） | P0 |
| US-17 | 作为**管理员**，我希望**处理举报工单**（查看、删除违规内容、标记办结） | P0 |
| US-18 | 作为**管理员**，我希望**封禁/解封违规用户账号** | P1 |
| US-19 | 作为**管理员**，我希望**维护关系图谱中的人物和连线数据** | P0 |
| US-20 | 作为**管理员**，我希望**查看操作日志** | P1 |
| US-21 | 作为**管理员**，我**不能**查看粉丝的私密消息（权限隔离） | P0 |

### 5.5 边界场景

| # | 用户故事 | 优先级 |
|---|---------|--------|
| US-22 | 新用户注册流程简单：用户名+密码+验证码+勾选协议 | P0 |
| US-23 | 点赞后按钮立即锁定防连点，20秒发送冷却 | P0 |
| US-24 | 所有删除操作有二次确认 | P1 |
| US-25 | 被拉黑用户登录时有明确提示 | P1 |

---

## 6 全局通用强制规则

（后端接口强制校验，前端仅做视觉提示）

1. **消息限流**：公开、私密消息统一 20 秒发送冷却，后端校验时间戳拦截

2. **点赞防抖与幂等**（新增）：
   - **前端防抖**：点击点赞按钮后立即 disabled，300ms 内忽略重复点击
   - **前端节流**：点赞/取消点赞操作间隔不得小于 1 秒
   - **后端幂等**：同一用户对同一消息，1 秒内的重复点赞/取消请求直接忽略返回，不做重复处理
   - 单用户对单条公开消息仅能点赞一次，支持点赞/取消；只展示点赞总量，不做热度排行

3. **私密数据隔离**：
   - 私密消息仅**发送人本人 + 主播（streamer）**有权读取
   - 私密回复仅**主播发出 + 目标用户**可见
   - **管理员（admin）无权读取任何私密消息和私密回复**
   - 后端通过 SQL + 角色鉴权中间件强制过滤，前端无法篡改

4. **安全规范**：
   - 所有用户输入内容增加 XSS 过滤
   - 聊天数据、用户行为永久入库留存
   - 文件上传权限仅对管理员和主播开放
   - **昵称敏感词过滤**：内置敏感词库，昵称包含敏感词时拒绝保存并提示
   - **昵称长度限制**：最长 10 个字符
   - **聊天消息字数限制**：单条消息最长 500 字，超出拒绝发送

5. **性能强制规则**（新增）：
   - 所有图片上传后自动压缩（质量降至 80%，最大宽度 1920px）
   - 音频文件上传后检查码率，超过 192kbps 自动转码压缩
   - 接口返回数据最小化，不返回不必要的字段
   - 前端图片使用懒加载（`loading="lazy"`）
   - 列表数据分页加载，每页不超过 20 条
   - ECharts 图谱渲染使用 `animation: false` 避免大数据量动画卡顿

---

## 7 功能需求（按优先级分级）

### P0 - Must-Have

#### 7.1 首页展示模块

| 功能项 | 描述 | 验收标准 |
|--------|------|---------|
| Banner 大图展示 | 顶部通栏展示管理员/Banner | Given 后台已上传 Banner，When 用户访问首页，Then 展示最新 Banner |
| 主播信息卡片 | 头像、名称、简介、标签 | Given 后台已配置，When 用户访问，Then 完整展示 |
| 获奖记录模块 | 卡片列表展示获奖信息 | Given 后台已录入（由主播/管理员上传），When 用户访问，Then 按时间倒序展示 |
| 图集 Tab 切换 | 二次元/三次元，等宽卡片，分页，大图预览 | 图片为**主播个人版权作品，无需标注来源**；分页每页 ≤ 20 张 |
| 活动日历模块 | 卡片展示活动时间、名称、简介 | Given 后台已录入，Then 正确展示 |
| 音乐播放模块 | 歌曲列表+封面，原生 Audio 播放 mp3 | 音频为**主播个人音乐作品，已授权使用**；音频文件压缩后传输 |
| 群像关系图谱 | ECharts 力导向图，中心主播+10 公会人物 | 数据由**管理员/主播在后台添加**；点击节点弹窗展示信息；渲染关闭动画避免卡顿 |

#### 7.2 聊天室核心功能

| 功能项 | 描述 | 验收标准 |
|--------|------|---------|
| 消息发送 | 公开/私密两种类型 | 单条消息 ≤ 500 字，超出拒绝发送并提示 |
| 20 秒发送冷却 | 发送后按钮锁定倒计时 | 20 秒内按钮锁定+倒计时显示 |
| 消息轮询刷新 | 每 3 秒轮询 | 新消息 ≤ 3 秒出现；页面切后台暂停，激活恢复 |
| 消息展示区分 | 公开消息有点赞+举报，私密消息标"仅主播可见" | 私密消息仅发送人+主播可见 |
| 主播私密回复 | 仅主播可回复私密消息，回复仅发送人可见 | 管理员无法进行私密回复 |
| **点赞防抖** | 前端点击后立即 disabled + 300ms 防抖 + 1s 节流；后端 1s 内重复请求忽略 | Given 用户快速连点，When 点击多次，Then 只触发一次点赞操作，无重复计数 |
| 举报功能 | 提交举报生成工单 | 消息不自动删除，等管理员处理 |

#### 7.3 个人中心

| 功能项 | 描述 | 验收标准 |
|--------|------|---------|
| 资料只读信息 | 登录账号、注册时间 | 不可修改 |
| 昵称修改 | 输入框修改，≤ 10 字 | Given 输入昵称，When 长度 > 10 字或含敏感词，Then 拒绝保存并提示原因 |
| 头像选择 | 弹窗选择系统头像库 | 用户无法上传图片 |
| 每日打卡 | 每日一次 | 当日已打卡则按钮置灰 |
| 打卡日历 | 当月日历，已打卡高亮，悬浮显示时间 | 仅展示本人数据 |

#### 7.4 认证系统

| 功能项 | 描述 | 验收标准 |
|--------|------|---------|
| 注册 | 用户名+密码+svg-captcha 验证码+**勾选用户协议** | Given 未勾选协议，When 提交注册，Then 提示"请先阅读并同意用户协议" |
| 登录 | 用户名+密码+JWT | 凭据错误不透露具体哪项错误 |
| 登出 | 清除 JWT | 跳转首页 |

#### 7.5 后台管理

| 功能项 | 描述 | 验收标准 |
|--------|------|---------|
| 站点基础配置 | Banner、主播资料、获奖记录 | 由主播/管理员上传维护 |
| 素材资源管理 | 图集、音源、头像池 | 上传时自动压缩 |
| 公共数据管理 | 活动日历、图谱人物、关系连线 | 由管理员/主播维护 |
| 聊天室管理 | 查看全部消息（含私密）、删除违规消息 | 注意：此功能仅主播可见私密消息内容，管理员仅可见公开消息 |
| 举报工单管理 | 待处理/已办结，查看详情、删除违规、标记办结 | — |
| 用户管理 | 查看注册用户、拉黑/解封 | — |

---

### P1 - Nice-to-Have

| 功能项 | 描述 | 验收标准 |
|--------|------|---------|
| 点赞动画 | 点赞数变化时微动画 | 轻量 CSS 动画，不影响性能 |
| 打卡连续天数 | 展示"已连续打卡 N 天" | 基于打卡记录计算 |
| 删除二次确认 | 弹窗确认 | 点击"确认"才执行 |
| 封禁提示 | 被拉黑用户登录时提示 | "账号已被封禁，请联系管理员" |
| 管理员操作日志 | 记录所有后台操作 | 操作类型+时间+对象 |
| 消息/打卡空状态 | 友好提示文案 | — |

---

### P2 - Future Considerations（V2.0 考虑）

| 功能项 | 说明 | 触发条件 |
|--------|------|---------|
| WebSocket 实时通信 | 替换轮询，降低请求压力 | 同时在线用户 > 500 时升级 |
| 粉丝等级体系 | 基于打卡天数、互动频率设计徽章 | 注册用户 > 200 |
| 移动端适配 | 响应式布局 | 移动端访问占比 > 30% |
| 第三方登录 | GitHub/Google OAuth | 有需求且成本可控 |

> 注意：粉丝私信互聊、数据导出已明确**永久不做**，不放入 V2.0 规划。

---

## 8 前台页面产品设计

### 页面 1：首页（/）

页面自上而下纵向排布区块：

1. **顶部通栏 Banner 大图展示区**

2. **主播信息卡片**：展示头像、名称、个人简介、身份标签，数据由后台配置

3. **获奖记录模块**：卡片列表纵向展示全部获奖信息（由主播/管理员上传）

4. **图集 Tab 切换模块**
    - Tab 选项：二次元图集 / 三次元实拍图集
    - 图片采用等宽卡片布局，**分页加载（每页 ≤ 20 张）**，图片懒加载
    - 点击图片唤起大图预览弹窗
    - 图片为**主播个人版权作品，无需标注来源**

5. **活动日历模块**：卡片展示活动时间、活动名称、活动简介

6. **音乐播放模块**：歌曲列表搭配封面图，点击使用浏览器原生 Audio 标签播放 mp3 音源（**主播个人音乐作品，已授权使用**）

7. **群像关系图谱板块**
    - 使用 ECharts 力导向图渲染，中心主播节点放大突出，其余 10 个公会人物分散排布
    - 数据由**管理员/主播在后台维护**
    - 渲染配置 `animation: false` 避免卡顿
    - 交互：点击人物节点弹窗展示头像+昵称+简介；点击空白处关闭

### 页面 2：聊天室（/chat），对话框流式布局

1. **上部：聊天消息展示容器**
    - 单条消息结构：头像 + 昵称 + 时间 + 正文
    - 公开消息：右下角点赞按钮（带防抖）+ 举报按钮
    - 私密消息：左上角灰色「仅主播可见」标签，只保留举报按钮
    - 主播回复区分：公开回复全员可见，私密回复仅发送人可见+「私密回复」标识

2. **下部：消息发送操作栏**
    - 单选：公开发送 / 仅主播可见
    - 输入框 + 发送按钮；发送后 20 秒倒计时锁定
    - 输入框限制 500 字，超出时显示字数提示

3. **轮询策略**：3 秒间隔；切后台暂停，激活恢复

### 页面 3：个人中心（/user）

#### 模块 A：资料设置
- 只读：登录账号、注册时间
- 昵称修改：≤ 10 字，敏感词过滤
- 头像选择：弹窗选择系统头像库

#### 模块 B：月度打卡日历
- 当月日历格子，已打卡高亮
- 每日一次，打卡后按钮置灰
- 悬浮显示精确时间

### 页面 4：登录 / 注册

- 注册：用户名 + 密码 + svg-captcha 验证码 + **勾选《用户协议》**
- 登录：用户名 + 密码
- JWT 维持登录状态

---

## 9 后台管理系统

### 侧边导航菜单

1. 站点基础配置：Banner、主播资料、获奖记录
2. 素材资源管理：二次元图集、三次元图集、歌曲音源、系统头像池
3. 公共数据管理：活动日历、图谱人物、人物关系连线
4. 聊天室管理：查看公开消息、删除违规消息
5. 举报工单管理：待处理/已办结
6. 用户管理：查看注册用户、拉黑/解封

### 权限隔离说明

- **主播（streamer）后台**：可看到以上全部菜单 + 私密消息管理入口
- **管理员（admin）后台**：可看到 1-6 菜单，但聊天室管理中**仅展示公开消息**，不可查看私密消息

### 统一交互规范

新增/编辑用弹窗表单；列表分页；删除二次确认。

---

## 10 成功指标

### 领先指标

| 指标 | 目标值 | 评估时间 |
|------|--------|---------|
| 注册用户数 | 30 天内 ≥ 50 | 上线 30 天 |
| 日均消息量 | ≥ 20 条/天 | 上线 14 天 |
| 打卡参与率 | ≥ 30% | 上线 30 天 |
| 图谱页面 UV 占比 | ≥ 40% | 上线 14 天 |
| **首屏加载时间** | < 2 秒 | 上线前 |
| **P95 接口响应** | < 500ms | 上线前 |

### 滞后指标

| 指标 | 目标值 | 评估时间 |
|------|--------|---------|
| 7 日留存率 | ≥ 40% | 上线 60 天 |
| 用户满意度 | 正面反馈为主 | 上线 90 天 |
| 月均举报工单 | < 10 条 | 上线 90 天 |
| 月均宕机 | < 1 次 | 上线 90 天 |
| **月均运营成本** | ≤ 100 元 | 持续监控 |

---

## 11 非功能设计

1. **前端性能**：静态资源 gzip 压缩、图片懒加载、路由懒加载、按需渲染
2. **文件压缩**：上传图片自动压缩（质量 80%，最大宽度 1920px）；音频超过 192kbps 自动转码
3. **安全**：管理员/主播操作日志；注册页底部《用户协议》勾选；XSS 过滤；bcrypt 加密
4. **性能**：轮询 3 秒刷新，无需 WebSocket；接口返回最小化数据；分页 ≤ 20 条/页
5. **接口规范**：RESTful，统一 `{ code, message, data }`，全局异常捕获
6. **跨域**：开发环境 localhost，生产环境限制指定域名

---

## 12 Open Questions（全部已解答）

| # | 问题 | 答案 | 状态 |
|---|------|------|------|
| Q1 | 获奖记录数据来源？ | **由主播/管理员在后台上传录入** | ✅ 已解决 |
| Q2 | 图谱人物和关系由谁提供？ | **由管理员/主播在后台添加维护** | ✅ 已解决 |
| Q3 | 图集图片版权？ | **主播个人版权作品，无需标注来源** | ✅ 已解决 |
| Q4 | 音乐版权？ | **主播个人音乐作品，已授权使用** | ✅ 已解决 |
| Q5 | 部署服务器方案？ | **开发阶段用个人电脑；上线后购买轻量服务器（推荐香港节点免备案）** | ✅ 已解决 |
| Q6 | 域名和备案？ | **域名在腾讯云/阿里云购买；选择香港节点服务器免备案** | ✅ 已解决 |
| Q7 | 图形验证码方案？ | **使用 `svg-captcha`（Node.js 库），纯后端生成 SVG，零成本** | ✅ 已解决 |
| Q8 | 昵称规则？ | **敏感词过滤 + 长度限制 10 字** | ✅ 已解决 |
| Q9 | 聊天消息限制？ | **单条 500 字** | ✅ 已解决 |
| Q10 | 用户免责协议？ | **注册时勾选同意；内容由开发者使用模板生成，声明禁止违规发言、账号责任自负、内容版权归主播** | ✅ 已解决 |

---

## 13 部署与成本方案（新增）

### 开发阶段

- 本地开发，MySQL + Redis 均在 Windows 本地运行
- **Navicat** 连接 MySQL，导入建表 SQL，可视化管理数据
- **Redis Insight** 监控 Redis 数据，调试缓存 key
- 前后端 localhost 联调

### 上线阶段（性价比方案）

| 项目 | 推荐方案 | 预估费用 |
|------|---------|---------|
| **服务器** | 腾讯云/阿里云轻量应用服务器（**香港节点**，免备案） | 2 核 2G：约 **24-50 元/月**（新人优惠更低） |
| **域名** | 腾讯云/阿里云注册，推荐 `.top` 或 `.com` | `.top` 首年 **1 元**；`.com` 首年约 50 元 |
| **CDN + 防护** | Cloudflare 免费版 | **0 元**（免费 CDN + DDoS 防护 + SSL） |
| **图片压缩** | 后端 `sharp` 库自动压缩 | **0 元**（纯计算） |
| **数据库** | MySQL 部署在同一台服务器 | **0 元**（共用资源） |

### 初期月均成本预估：≤ 50-100 元

### 服务器容量参考

| 同时在线用户 | 推荐配置 | 月费参考 |
|------------|---------|---------|
| < 50 人 | 1 核 1G | 约 20 元 |
| 50-200 人 | 2 核 2G | 约 30-50 元 |
| 200-1000 人 | 2 核 4G | 约 60-100 元 |
| 1000-5000 人 | 4 核 8G | 约 150-300 元 |
| 5000-10000 人 | 4 核 16G + CDN | 约 300-600 元 |

> V1.0 初期推荐 **2 核 2G 香港节点**，足够支撑 200 人同时在线。用户增长后再升配。

### 关于备案

- 个人开发者可以备案，但流程繁琐（7-20 天审核，需要个人证件、网站截图等）
- **推荐方案**：选择**香港节点服务器**，直接**免备案**，即买即用
- 如果后续确实需要备案（比如要用国内 CDN 加速），再走备案流程

---

## 14 技术选型（已确认，2026-07-23 更新）

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Vue 3 + Vite | 路由懒加载 |
| **样式方案** | **纯 CSS（模块化 / BEM 命名）** | ~~Tailwind CSS~~ → 改为纯 CSS，避免 class 堆积，更好维护 |
| 可视化 | ECharts | 关闭动画渲染，避免卡顿 |
| 后端 | Express.js (Node.js) | RESTful API |
| 包管理 | pnpm | 既定规则 |
| ORM | Prisma | 类型安全，从现有数据库 pull schema |
| **数据库** | **MySQL** | 本地开发；建表 role 字段区分 fan/admin/streamer |
| **缓存** | **Redis 8.2.5（Windows）+ Redis Insight** | 验证码存储（TTL 5min）、点赞幂等（TTL 1s）、消息冷却（TTL 20s）、JWT 黑名单 |
| **数据库管理** | **Navicat** | 可视化查看和管理数据、导入建表 SQL |
| 认证 | JWT + bcrypt | — |
| 验证码 | svg-captcha | 纯后端生成 SVG，零成本 |
| 图片处理 | sharp | 上传自动压缩 |
| 部署 | 香港轻量服务器 + Cloudflare | 免备案 |

### 开发模式

- **核心业务逻辑**：手写（注册登录、权限体系、聊天室、打卡、私密消息等）
- **AI 辅助生成**：样式代码、CRUD 模板代码、配置文件、测试数据
- **目标**：沉淀整套开发流程，作为后续接单复用的 SOP 模板

---

## 15 数据模型概要（V2 修订）

核心实体（ER 图在阶段 1 详细设计）：

- **users**：id, username, password_hash, nickname, avatar_id, **role（fan/admin/streamer）**, status, created_at
- **banners**：Banner 管理表
- **streamer_info**：主播资料表（单条记录）
- **awards**：获奖记录表（由主播/管理员录入）
- **gallery_images**：图集表（含分类：二次元/三次元）
- **songs**：音乐表
- **activities**：活动日历表
- **graph_characters**：图谱人物表（由管理员/主播维护）
- **graph_relations**：图谱关系连线表
- **messages**：聊天消息表（type: public/private; sender_id, reply_by_streamer_id）
- **private_replies**：主播私密回复表（**仅 streamer 可创建，仅发送人+streamer 可见**）
- **likes**：点赞记录（user_id + message_id 唯一约束；**后端 1s 幂等校验**）
- **reports**：举报工单表
- **check_ins**：打卡记录（user_id + date 唯一约束）
- **avatars**：系统头像池表
- **admin_logs**：操作日志表
- **sensitive_words**：敏感词库表（用于昵称过滤）

### 关键权限字段设计

```
messages 表：
- type: ENUM('public', 'private')
- sender_id: FK → users.id
- visible_to: sender_id + streamer（后端 SQL 强制过滤）

private_replies 表：
- message_id: FK → messages.id
- streamer_id: FK → users.id（role = 'streamer'）
- target_user_id: FK → users.id（原消息发送人）
- content: 回复内容
- 可见范围：target_user_id + streamer_id（admin 不可见）
```

---

## 16 开发执行顺序

| 阶段 | 内容 | 预估周期 | 依赖 |
|------|------|---------|------|
| 1 | ER 图 + MySQL 建表 SQL | 1-2 天 | PRD 定稿 |
| 2 | 脚手架初始化（统一返回、异常捕获、跨域） | 1 天 | 技术选型确认 |
| 3 | 权限体系（注册、登录、svg-captcha、JWT、**角色三级中间件**） | 2-3 天 | 阶段 2 |
| 4 | 后台全部接口+管理页面 | 3-5 天 | 阶段 3 |
| 5 | 前台静态页面（首页模块、图谱） | 3-5 天 | 阶段 2（可与 4 并行） |
| 6 | 个人中心（头像、昵称含敏感词过滤、打卡） | 2 天 | 阶段 3 |
| 7 | 聊天室（限流、**点赞防抖+幂等**、轮询、举报、**主播私密回复**） | 3-4 天 | 阶段 3 |
| 8 | 联调、美化、BUG 修复、测试数据、部署 | 3-5 天 | 阶段 4-7 |
| 9 | 复盘 + SOP 模板 | 1-2 天 | 阶段 8 |

**预估总周期：19-29 天**

---

> （注：部分内容可能由 AI 生成）

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
