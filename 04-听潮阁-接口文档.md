---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 1822362781095152_0/project_7665287858529468722-files/PRD/听潮阁-接口文档.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 1822362781095152#1784791131958
    ReservedCode2: ""
---
# 听潮阁虚拟主播粉丝官网 - 后端接口文档 V1.0

> 版本：V1.0 | 日期：2026-07-23 | 基于 PRD V2.1 + 建表SQL V1.0（17张表）

---

## 全局约定

### 基础信息
- **基础路径**: `/api`
- **协议**: HTTP/HTTPS
- **数据格式**: JSON（`Content-Type: application/json`）
- **认证方式**: JWT Bearer Token（Header: `Authorization: Bearer <token>`）

### 统一返回格式

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

### 错误码约定

| code | 含义 | 说明 |
|------|------|------|
| 0 | 成功 | 请求成功 |
| 400 | 参数错误 | 缺少必填参数、格式不正确、校验不通过 |
| 401 | 未登录 | Token 缺失、过期或已加入黑名单 |
| 403 | 权限不足 | 角色不满足接口要求 |
| 404 | 不存在 | 请求的资源不存在 |
| 409 | 冲突 | 数据重复（如用户名已存在） |
| 429 | 请求过于频繁 | 限流触发（消息冷却等） |
| 500 | 服务器错误 | 服务端内部异常 |

### 分页参数约定

所有分页接口统一使用以下参数：
- `page` (number, 可选, 默认=1): 页码
- `pageSize` (number, 可选, 默认=20): 每页条数，最大20

分页返回格式：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [],
    "pagination": {
      "page": 1,
      "pageSize": 20,
      "total": 100,
      "totalPages": 5
    }
  }
}
```

### 角色标识

| 标识 | 角色 | 说明 |
|------|------|------|
| `fan` | 粉丝 | 注册粉丝用户 |
| `admin` | 管理员 | 后台管理 |
| `streamer` | 主播 | 最高权限，可处理私密消息 |

### Redis 使用场景汇总

| Key 模式 | TTL | 用途 |
|----------|-----|------|
| `sess_id:svg_captcha` | 5min | 图形验证码存储 |
| `like:{user_id}:{message_id}` | 1s | 点赞幂等控制 |
| `rate_limit:msg:{user_id}` | 20s | 消息发送冷却 |
| `jwt_blacklist:{jti}` | JWT剩余有效期 | JWT 黑名单（登出/封禁） |

---

## 接口列表

---

## 1. 认证模块

---

### 1.1 获取图形验证码

**接口名称**: 获取图形验证码

**请求**: `GET /api/auth/captcha`

**是否需要登录**: 否

**角色限制**: 无

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "svg": "<svg xmlns=...>...</svg>",
    "captchaId": "uuid-session-id"
  }
}
```

**业务逻辑**:
1. 使用 `svg-captcha` 库生成 SVG 图形验证码
2. 将验证码文本存入 Redis，key = `{captchaId}:svg_captcha`，TTL = 5分钟
3. 返回 SVG 字符串和 captchaId 给前端

**特殊规则**: 
- 使用 Redis 存储验证码
- 每次请求生成新的验证码，旧验证码自动失效

---

### 1.2 用户注册

**接口名称**: 用户注册

**请求**: `POST /api/auth/register`

**是否需要登录**: 否

**角色限制**: 无

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 登录账号，3-50字符，字母开头，仅含字母数字下划线 |
| password | string | 是 | 密码，6-20字符 |
| captchaId | string | 是 | 验证码会话ID |
| captchaText | string | 是 | 用户输入的验证码文本 |
| agreement | boolean | 是 | 是否勾选用户协议，必须为true |

**返回数据**:
```json
{
  "code": 0,
  "message": "注册成功",
  "data": {
    "userId": 10001
  }
}
```

**业务逻辑**:
1. 校验 agreement 必须为 true
2. 从 Redis 取出 `{captchaId}:svg_captcha` 验证验证码，校验后删除
3. 校验 username 唯一性（查 users 表 uk_username）
4. 使用 bcrypt 加密密码（saltRounds=10）
5. 插入 users 表，role 默认 `fan`，status 默认 `active`，nickname 默认与 username 相同

**特殊规则**: 
- 使用 Redis 校验验证码
- 凭据错误不透露具体哪项错误
- 注册后不自动登录，需跳转登录页

---

### 1.3 用户登录

**接口名称**: 用户登录

**请求**: `POST /api/auth/login`

**是否需要登录**: 否

**角色限制**: 无

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 登录账号 |
| password | string | 是 | 密码 |

**返回数据**:
```json
{
  "code": 0,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "expiresIn": 86400,
    "user": {
      "id": 10001,
      "username": "fan001",
      "nickname": "小潮粉丝",
      "avatar": "https://cdn.example.com/avatars/1.jpg",
      "role": "fan"
    }
  }
}
```

**业务逻辑**:
1. 查询 users 表，匹配 username
2. 使用 bcrypt.compare 校验密码
3. 检查 status 字段，若为 `banned` 返回 `{ code: 403, message: "账号已被封禁，请联系管理员" }`
4. 签发 JWT（payload: { userId, role, jti }，有效期 24h）
5. 更新 users 表 updated_at

**特殊规则**: 
- 密码错误统一返回"用户名或密码错误"，不区分
- 被封禁用户登录时返回明确提示

---

### 1.4 用户登出

**接口名称**: 用户登出

**请求**: `POST /api/auth/logout`

**是否需要登录**: 是

**角色限制**: 无

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "登出成功",
  "data": null
}
```

**业务逻辑**:
1. 从 JWT 中解析出 jti 和剩余有效期
2. 将 `jwt_blacklist:{jti}` 写入 Redis，TTL = JWT 剩余有效期
3. 前端清除 token

**特殊规则**: 
- 使用 Redis 实现 JWT 黑名单
- 登出后已签发的 token 立即失效

---

### 1.5 获取当前用户信息

**接口名称**: 获取当前登录用户信息

**请求**: `GET /api/auth/me`

**是否需要登录**: 是

**角色限制**: 无

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 10001,
    "username": "fan001",
    "nickname": "小潮粉丝",
    "avatar": "https://cdn.example.com/avatars/1.jpg",
    "avatarId": 3,
    "role": "fan",
    "createdAt": "2026-07-01T10:00:00.000Z"
  }
}
```

**业务逻辑**:
1. 从 JWT 中获取 userId
2. 查询 users 表，JOIN avatars 获取头像 URL
3. 返回用户基本信息（不返回 password_hash）

---

## 2. 用户模块

---

### 2.1 修改昵称

**接口名称**: 修改展示昵称

**请求**: `PUT /api/user/nickname`

**是否需要登录**: 是

**角色限制**: fan / admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| nickname | string | 是 | 新昵称，1-10字符 |

**返回数据**:
```json
{
  "code": 0,
  "message": "昵称修改成功",
  "data": {
    "nickname": "小潮粉丝"
  }
}
```

**业务逻辑**:
1. 校验 nickname 长度 ≤ 10 字符
2. 查询 sensitive_words 表，逐词匹配昵称是否包含敏感词
3. 若包含敏感词，返回 `{ code: 400, message: "昵称包含敏感词，请修改" }`
4. 更新 users 表 nickname 字段

**特殊规则**: 
- 昵称长度 ≤ 10字
- 敏感词过滤（查 sensitive_words 表）
- XSS 过滤

---

### 2.2 修改头像

**接口名称**: 选择系统头像

**请求**: `PUT /api/user/avatar`

**是否需要登录**: 是

**角色限制**: fan / admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| avatarId | number | 是 | 系统头像池中的头像ID |

**返回数据**:
```json
{
  "code": 0,
  "message": "头像修改成功",
  "data": {
    "avatar": "https://cdn.example.com/avatars/5.jpg"
  }
}
```

**业务逻辑**:
1. 查询 avatars 表确认 avatarId 存在
2. 更新 users 表的 avatar_id 字段
3. 返回新的头像 URL

**特殊规则**: 
- 粉丝不能上传头像，只能从系统头像池中选择
- avatarId 不存在时返回 404

---

### 2.3 获取头像池列表

**接口名称**: 获取系统头像池

**请求**: `GET /api/user/avatars`

**是否需要登录**: 是

**角色限制**: fan / admin / streamer

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "id": 1, "url": "https://cdn.example.com/avatars/1.jpg" },
    { "id": 2, "url": "https://cdn.example.com/avatars/2.jpg" }
  ]
}
```

**业务逻辑**:
1. 查询 avatars 表，按 sort_order DESC 排序
2. 返回全部头像列表

---

## 3. 首页-Banner

---

### 3.1 前台获取 Banner 列表

**接口名称**: 获取首页 Banner 列表

**请求**: `GET /api/banners`

**是否需要登录**: 否

**角色限制**: 无

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "title": "听潮阁周年庆",
      "imageUrl": "https://cdn.example.com/banners/1.jpg",
      "linkUrl": "https://example.com/event",
      "sortOrder": 100
    }
  ]
}
```

**业务逻辑**:
1. 查询 banners 表，条件 `is_visible = 1`，按 `sort_order DESC` 排序
2. 返回所有可见 Banner

**特殊规则**: 
- 仅返回 is_visible=1 的记录
- 不返回 created_at/updated_at 等非必要字段

---

### 3.2 后台获取 Banner 列表（分页）

**接口名称**: 后台获取全部 Banner（含隐藏）

**请求**: `GET /api/admin/banners`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码，默认1 |
| pageSize | number | 否 | 每页条数，默认20 |

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "title": "听潮阁周年庆",
        "imageUrl": "https://cdn.example.com/banners/1.jpg",
        "linkUrl": "https://example.com/event",
        "sortOrder": 100,
        "isVisible": true,
        "createdAt": "2026-07-01T10:00:00.000Z",
        "updatedAt": "2026-07-10T15:30:00.000Z"
      }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 5, "totalPages": 1 }
  }
}
```

**业务逻辑**:
1. 鉴权：检查角色为 admin 或 streamer
2. 分页查询 banners 表全部记录（含隐藏），按 sort_order DESC
3. 记录操作日志

---

### 3.3 新增 Banner

**接口名称**: 新增 Banner

**请求**: `POST /api/admin/banners`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | 否 | Banner标题，≤100字符 |
| imageUrl | string | 是 | Banner图片URL |
| linkUrl | string | 否 | 点击跳转链接，≤500字符 |
| sortOrder | number | 否 | 排序权重，默认0 |
| isVisible | boolean | 否 | 是否显示，默认true |

**返回数据**:
```json
{
  "code": 0,
  "message": "Banner创建成功",
  "data": { "id": 6 }
}
```

**业务逻辑**:
1. 鉴权 + XSS 过滤
2. 插入 banners 表
3. 写入 admin_logs（action: `create_banner`）

---

### 3.4 编辑 Banner

**接口名称**: 编辑 Banner

**请求**: `PUT /api/admin/banners/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | 否 | Banner标题 |
| imageUrl | string | 否 | Banner图片URL |
| linkUrl | string | 否 | 跳转链接 |
| sortOrder | number | 否 | 排序权重 |
| isVisible | boolean | 否 | 是否显示 |

**返回数据**:
```json
{
  "code": 0,
  "message": "Banner更新成功",
  "data": null
}
```

**业务逻辑**:
1. 鉴权 + 校验 Banner 存在
2. 更新 banners 表对应字段
3. 写入 admin_logs（action: `update_banner`）

---

### 3.5 删除 Banner

**接口名称**: 删除 Banner

**请求**: `DELETE /api/admin/banners/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "Banner删除成功",
  "data": null
}
```

**业务逻辑**:
1. 鉴权 + 校验 Banner 存在
2. 物理删除 banners 表记录
3. 写入 admin_logs（action: `delete_banner`）

---

## 4. 首页-主播资料

---

### 4.1 前台获取主播资料

**接口名称**: 获取主播资料

**请求**: `GET /api/streamer-info`

**是否需要登录**: 否

**角色限制**: 无

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "听潮阁小潮",
    "avatarUrl": "https://cdn.example.com/streamer/avatar.jpg",
    "tags": ["虚拟主播", "歌势", "游戏"],
    "bio": "这里是听潮阁小潮的个人简介..."
  }
}
```

**业务逻辑**:
1. 查询 streamer_info 表（仅一条记录）
2. tags 字段逗号分隔转数组返回

---

### 4.2 后台获取主播资料

**接口名称**: 后台获取主播资料（完整版）

**请求**: `GET /api/admin/streamer-info`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "听潮阁小潮",
    "avatarUrl": "https://cdn.example.com/streamer/avatar.jpg",
    "tags": "虚拟主播,歌势,游戏",
    "bio": "这里是听潮阁小潮的个人简介...",
    "updatedAt": "2026-07-20T10:00:00.000Z"
  }
}
```

**业务逻辑**:
1. 鉴权 + 查询 streamer_info 表全部字段

---

### 4.3 编辑主播资料

**接口名称**: 编辑主播资料

**请求**: `PUT /api/admin/streamer-info`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | string | 否 | 主播名称，≤50字符 |
| avatarUrl | string | 否 | 主播头像URL |
| tags | string | 否 | 身份标签，逗号分隔 |
| bio | string | 否 | 个人简介 |

**返回数据**:
```json
{
  "code": 0,
  "message": "主播资料更新成功",
  "data": null
}
```

**业务逻辑**:
1. 鉴权 + XSS 过滤
2. 更新 streamer_info 表（全站唯一记录，id=1）
3. 写入 admin_logs（action: `update_streamer_info`）

---

## 5. 首页-获奖记录

---

### 5.1 前台获取获奖记录列表

**接口名称**: 获取获奖记录列表

**请求**: `GET /api/awards`

**是否需要登录**: 否

**角色限制**: 无

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "title": "B站年度最佳虚拟主播",
      "description": "2026年B站年度评选...",
      "imageUrl": "https://cdn.example.com/awards/1.jpg",
      "awardDate": "2026-06-15",
      "sortOrder": 100
    }
  ]
}
```

**业务逻辑**:
1. 查询 awards 表，按 `award_date DESC` 排序
2. 返回全部获奖记录

---

### 5.2 后台获取获奖记录列表（分页）

**接口名称**: 后台获取获奖记录（分页）

**请求**: `GET /api/admin/awards`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query): page, pageSize

**返回数据**: 标准分页格式，list 包含完整字段 + createdAt, updatedAt

---

### 5.3 新增获奖记录

**接口名称**: 新增获奖记录

**请求**: `POST /api/admin/awards`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | 是 | 奖项名称，≤200字符 |
| description | string | 否 | 奖项描述 |
| imageUrl | string | 否 | 相关图片URL |
| awardDate | string | 否 | 获奖日期，格式 YYYY-MM-DD |
| sortOrder | number | 否 | 排序权重，默认0 |

**返回数据**:
```json
{
  "code": 0,
  "message": "获奖记录创建成功",
  "data": { "id": 10 }
}
```

**业务逻辑**:
1. 鉴权 + XSS 过滤 + 日期格式校验
2. 插入 awards 表
3. 写入 admin_logs（action: `create_award`）

---

### 5.4 编辑获奖记录

**接口名称**: 编辑获奖记录

**请求**: `PUT /api/admin/awards/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body): 同新增，均可选

**返回数据**: `{ "code": 0, "message": "获奖记录更新成功", "data": null }`

**业务逻辑**:
1. 鉴权 + 校验记录存在 + 更新 + 写日志

---

### 5.5 删除获奖记录

**接口名称**: 删除获奖记录

**请求**: `DELETE /api/admin/awards/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**: `{ "code": 0, "message": "获奖记录删除成功", "data": null }`

**业务逻辑**: 鉴权 + 删除 + 写 admin_logs

---

## 6. 首页-图集

---

### 6.1 前台获取图集（分页+分类）

**接口名称**: 获取图集列表

**请求**: `GET /api/gallery`

**是否需要登录**: 否

**角色限制**: 无

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| category | string | 否 | 分类筛选：二次元 / 三次元，不传返回全部 |
| page | number | 否 | 页码，默认1 |
| pageSize | number | 否 | 每页条数，默认20，最大20 |

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "category": "二次元",
        "url": "https://cdn.example.com/gallery/1.jpg",
        "title": "2026年春新衣装",
        "sortOrder": 100
      }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 45, "totalPages": 3 }
  }
}
```

**业务逻辑**:
1. 查询 gallery_images 表，可选 category 过滤
2. 按 sort_order DESC 排序
3. 分页返回
4. 命中索引 `idx_category_sort`

---

### 6.2 后台获取图集列表（分页）

**接口名称**: 后台获取图集列表

**请求**: `GET /api/admin/gallery`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query): category(可选), page, pageSize

**返回数据**: 标准分页格式

---

### 6.3 新增图集图片

**接口名称**: 新增图集图片

**请求**: `POST /api/admin/gallery`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| category | string | 是 | 分类：二次元 / 三次元 |
| url | string | 是 | 图片URL（上传后自动压缩） |
| title | string | 否 | 图片标题，≤100字符 |
| sortOrder | number | 否 | 排序权重，默认0 |

**返回数据**:
```json
{
  "code": 0,
  "message": "图集图片创建成功",
  "data": { "id": 46 }
}
```

**业务逻辑**:
1. 鉴权 + 校验 category 枚举值
2. 插入 gallery_images 表
3. 写入 admin_logs（action: `create_gallery_image`）

---

### 6.4 编辑图集图片

**接口名称**: 编辑图集图片

**请求**: `PUT /api/admin/gallery/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body): 同新增，均可选

**返回数据**: `{ "code": 0, "message": "图集图片更新成功", "data": null }`

**业务逻辑**: 鉴权 + 更新 + 写日志

---

### 6.5 删除图集图片

**接口名称**: 删除图集图片

**请求**: `DELETE /api/admin/gallery/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**: `{ "code": 0, "message": "图集图片删除成功", "data": null }`

**业务逻辑**: 鉴权 + 删除 + 写日志

---

## 7. 首页-音乐

---

### 7.1 前台获取歌曲列表

**接口名称**: 获取歌曲列表

**请求**: `GET /api/songs`

**是否需要登录**: 否

**角色限制**: 无

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "title": "潮声",
      "artist": "听潮阁小潮",
      "audioUrl": "https://cdn.example.com/songs/1.mp3",
      "coverUrl": "https://cdn.example.com/songs/cover/1.jpg",
      "sortOrder": 100
    }
  ]
}
```

**业务逻辑**:
1. 查询 songs 表，按 sort_order DESC 排序
2. 返回全部歌曲列表

---

### 7.2 后台获取歌曲列表（分页）

**接口名称**: 后台获取歌曲列表

**请求**: `GET /api/admin/songs`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query): page, pageSize

**返回数据**: 标准分页格式

---

### 7.3 新增歌曲

**接口名称**: 新增歌曲

**请求**: `POST /api/admin/songs`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | 是 | 歌曲名称，≤200字符 |
| artist | string | 否 | 演唱者/创作者 |
| audioUrl | string | 是 | 音频文件URL（超192kbps自动转码） |
| coverUrl | string | 否 | 歌曲封面图URL |
| sortOrder | number | 否 | 排序权重，默认0 |

**返回数据**:
```json
{
  "code": 0,
  "message": "歌曲创建成功",
  "data": { "id": 5 }
}
```

**业务逻辑**:
1. 鉴权 + XSS 过滤
2. 插入 songs 表
3. 写入 admin_logs（action: `create_song`）

---

### 7.4 编辑歌曲

**接口名称**: 编辑歌曲

**请求**: `PUT /api/admin/songs/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body): 同新增，均可选

**返回数据**: `{ "code": 0, "message": "歌曲更新成功", "data": null }`

**业务逻辑**: 鉴权 + 更新 + 写日志

---

### 7.5 删除歌曲

**接口名称**: 删除歌曲

**请求**: `DELETE /api/admin/songs/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**: `{ "code": 0, "message": "歌曲删除成功", "data": null }`

**业务逻辑**: 鉴权 + 删除 + 写日志

---

## 8. 首页-活动日历

---

### 8.1 前台获取活动列表

**接口名称**: 获取活动日历列表

**请求**: `GET /api/activities`

**是否需要登录**: 否

**角色限制**: 无

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "title": "听潮阁三周年纪念直播",
      "description": "三周年特别直播活动...",
      "coverUrl": "https://cdn.example.com/activities/1.jpg",
      "startTime": "2026-08-15T20:00:00.000Z",
      "endTime": "2026-08-15T23:00:00.000Z",
      "sortOrder": 100
    }
  ]
}
```

**业务逻辑**:
1. 查询 activities 表，按 `start_time DESC` 排序
2. 返回全部活动列表

---

### 8.2 后台获取活动列表（分页）

**接口名称**: 后台获取活动列表

**请求**: `GET /api/admin/activities`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query): page, pageSize

**返回数据**: 标准分页格式

---

### 8.3 新增活动

**接口名称**: 新增活动

**请求**: `POST /api/admin/activities`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | 是 | 活动名称，≤200字符 |
| description | string | 否 | 活动简介 |
| coverUrl | string | 否 | 活动封面图URL |
| startTime | string | 是 | 开始时间，ISO 8601格式 |
| endTime | string | 否 | 结束时间，ISO 8601格式 |
| sortOrder | number | 否 | 排序权重，默认0 |

**返回数据**:
```json
{
  "code": 0,
  "message": "活动创建成功",
  "data": { "id": 8 }
}
```

**业务逻辑**:
1. 鉴权 + XSS 过滤 + 时间格式校验
2. 插入 activities 表
3. 写入 admin_logs（action: `create_activity`）

---

### 8.4 编辑活动

**接口名称**: 编辑活动

**请求**: `PUT /api/admin/activities/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body): 同新增，均可选

**返回数据**: `{ "code": 0, "message": "活动更新成功", "data": null }`

**业务逻辑**: 鉴权 + 更新 + 写日志

---

### 8.5 删除活动

**接口名称**: 删除活动

**请求**: `DELETE /api/admin/activities/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**: `{ "code": 0, "message": "活动删除成功", "data": null }`

**业务逻辑**: 鉴权 + 删除 + 写日志

---

## 9. 图谱

---

### 9.1 前台获取图谱数据

**接口名称**: 获取完整图谱数据（人物+关系）

**请求**: `GET /api/graph`

**是否需要登录**: 否

**角色限制**: 无

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "characters": [
      {
        "id": 1,
        "name": "听潮阁小潮",
        "avatarUrl": "https://cdn.example.com/characters/1.jpg",
        "bio": "听潮阁核心主播",
        "isCenter": true
      },
      {
        "id": 2,
        "name": "阿浪",
        "avatarUrl": "https://cdn.example.com/characters/2.jpg",
        "bio": "公会成员",
        "isCenter": false
      }
    ],
    "relations": [
      {
        "id": 1,
        "fromCharacterId": 1,
        "toCharacterId": 2,
        "relationLabel": "好友"
      }
    ]
  }
}
```

**业务逻辑**:
1. 查询 graph_characters 全部人物
2. 查询 graph_relations 全部关系
3. 合并返回（数据量 ≤ 11人物 + 关系，无需分页）

---

### 9.2 后台获取人物列表

**接口名称**: 后台获取图谱人物列表

**请求**: `GET /api/admin/graph/characters`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query): page, pageSize

**返回数据**: 标准分页格式

---

### 9.3 新增图谱人物

**接口名称**: 新增图谱人物

**请求**: `POST /api/admin/graph/characters`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | string | 是 | 人物名称，≤50字符 |
| avatarUrl | string | 否 | 人物头像URL |
| bio | string | 否 | 人物简介 |
| isCenter | boolean | 否 | 是否为中心节点，默认false |
| sortOrder | number | 否 | 排序权重，默认0 |

**返回数据**:
```json
{
  "code": 0,
  "message": "人物创建成功",
  "data": { "id": 11 }
}
```

**业务逻辑**:
1. 鉴权 + XSS 过滤
2. 插入 graph_characters 表
3. 写入 admin_logs（action: `create_graph_character`）

**特殊规则**: 
- 全站最多允许1个 isCenter=true 的中心节点，新增中心节点时自动将原中心节点置为 false

---

### 9.4 编辑图谱人物

**接口名称**: 编辑图谱人物

**请求**: `PUT /api/admin/graph/characters/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body): 同新增，均可选

**返回数据**: `{ "code": 0, "message": "人物更新成功", "data": null }`

**业务逻辑**: 鉴权 + 更新 + 写日志

---

### 9.5 删除图谱人物

**接口名称**: 删除图谱人物

**请求**: `DELETE /api/admin/graph/characters/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**: `{ "code": 0, "message": "人物删除成功", "data": null }`

**业务逻辑**:
1. 鉴权 + 删除 graph_characters 表记录
2. 级联删除关联的 graph_relations（外键 ON DELETE CASCADE）
3. 写入 admin_logs（action: `delete_graph_character`）

---

### 9.6 后台获取关系列表

**接口名称**: 后台获取图谱关系列表

**请求**: `GET /api/admin/graph/relations`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query): page, pageSize

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "fromCharacterId": 1,
        "fromCharacterName": "听潮阁小潮",
        "toCharacterId": 2,
        "toCharacterName": "阿浪",
        "relationLabel": "好友",
        "sortOrder": 0,
        "createdAt": "2026-07-01T10:00:00.000Z"
      }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 15, "totalPages": 1 }
  }
}
```

**业务逻辑**:
1. 鉴权
2. 查询 graph_relations JOIN graph_characters 获取人物名称
3. 分页返回

---

### 9.7 新增关系连线

**接口名称**: 新增图谱关系

**请求**: `POST /api/admin/graph/relations`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| fromCharacterId | number | 是 | 起始人物ID |
| toCharacterId | number | 是 | 目标人物ID |
| relationLabel | string | 是 | 关系标签，≤100字符 |
| sortOrder | number | 否 | 排序权重，默认0 |

**返回数据**:
```json
{
  "code": 0,
  "message": "关系创建成功",
  "data": { "id": 16 }
}
```

**业务逻辑**:
1. 鉴权 + 校验两个人物ID均存在
2. 校验 fromCharacterId ≠ toCharacterId（不能自关联）
3. 插入 graph_relations 表
4. 写入 admin_logs（action: `create_graph_relation`）

---

### 9.8 删除关系连线

**接口名称**: 删除图谱关系

**请求**: `DELETE /api/admin/graph/relations/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**: `{ "code": 0, "message": "关系删除成功", "data": null }`

**业务逻辑**: 鉴权 + 删除 + 写日志

---

## 10. 聊天室

---

### 10.1 获取公开消息列表（轮询用）

**接口名称**: 获取公开消息列表

**请求**: `GET /api/messages/public`

**是否需要登录**: 否（游客可浏览）

**角色限制**: 无

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| before | string | 否 | 获取此时间之前的消息（ISO 8601），用于加载更多 |
| limit | number | 否 | 条数，默认20，最大20 |

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1001,
      "senderId": 10001,
      "senderNickname": "小潮粉丝",
      "senderAvatar": "https://cdn.example.com/avatars/1.jpg",
      "content": "大家好！",
      "type": "public",
      "likeCount": 5,
      "isLiked": false,
      "createdAt": "2026-07-23T10:00:00.000Z"
    }
  ]
}
```

**业务逻辑**:
1. 查询 messages 表，条件 `type = 'public'`，按 `created_at DESC` 排序
2. 若传 before，则追加 `WHERE created_at < before`
3. 若用户已登录，通过 likes 表查询当前用户是否已点赞每条消息（isLiked 字段）
4. JOIN users 获取发送人昵称和头像
5. 命中索引 `idx_type_created`

**特殊规则**: 
- 轮询场景（前端每3秒调用），使用 before 参数避免重复数据
- 游客也能查看公开消息，但 isLiked 始终为 false

---

### 10.2 获取我的私密消息列表

**接口名称**: 获取我的私密消息

**请求**: `GET /api/messages/private`

**是否需要登录**: 是

**角色限制**: fan（仅粉丝）

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码，默认1 |
| pageSize | number | 否 | 每页条数，默认20 |

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 2001,
        "senderId": 10001,
        "content": "主播你好，想对你说...",
        "type": "private",
        "likeCount": 0,
        "createdAt": "2026-07-23T10:00:00.000Z"
      }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 3, "totalPages": 1 }
  }
}
```

**业务逻辑**:
1. 鉴权：角色必须为 fan
2. 查询 messages 表，条件 `type = 'private' AND sender_id = {当前用户ID}`
3. 按 created_at DESC 排序，分页返回
4. admin 和 streamer 不可调用此接口（有各自专用接口）

**特殊规则**: 
- fan 只能看到自己发的私密消息

---

### 10.3 发送消息

**接口名称**: 发送消息（公开/私密）

**请求**: `POST /api/messages`

**是否需要登录**: 是

**角色限制**: fan / admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| content | string | 是 | 消息内容，1-500字符 |
| type | string | 是 | 消息类型：public / private |

**返回数据**:
```json
{
  "code": 0,
  "message": "发送成功",
  "data": {
    "id": 1002,
    "content": "今天也要加油！",
    "type": "public",
    "likeCount": 0,
    "createdAt": "2026-07-23T10:01:00.000Z"
  }
}
```

**业务逻辑**:
1. 校验 content 长度 1-500 字符
2. XSS 过滤
3. **Redis 限流检查**：检查 `rate_limit:msg:{user_id}` 是否存在
   - 若存在，返回 `{ code: 429, message: "发送过于频繁，请20秒后再试" }`
4. 插入 messages 表（sender_id = 当前用户ID）
5. **写入 Redis**：设置 `rate_limit:msg:{user_id} = 1`，TTL = 20秒

**特殊规则**: 
- **使用 Redis** 实现 20s 发送冷却
- 公开消息和私密消息共用同一个冷却 key
- 消息字数限制 ≤ 500字

---

### 10.4 点赞消息

**接口名称**: 点赞公开消息

**请求**: `POST /api/messages/:id/like`

**是否需要登录**: 是

**角色限制**: fan / admin / streamer

**请求参数**: 无（id 为路径参数，消息ID）

**返回数据**:
```json
{
  "code": 0,
  "message": "点赞成功",
  "data": {
    "likeCount": 6
  }
}
```

**业务逻辑**:
1. 校验消息存在且 type = 'public'（私密消息不可点赞）
2. **Redis 幂等检查**：检查 `like:{user_id}:{message_id}` 是否存在
   - 若存在，直接返回当前 like_count，不做任何处理（`code: 0`）
3. 查询 likes 表 `uk_user_message` 唯一索引
   - 若已存在记录，返回 `code: 409, message: "已点赞过"`
4. 插入 likes 表
5. messages 表 like_count + 1
6. **写入 Redis**：设置 `like:{user_id}:{message_id} = 1`，TTL = 1秒

**特殊规则**: 
- **使用 Redis**（1s TTL）+ **MySQL UNIQUE KEY** 双重幂等保障
- 仅公开消息可点赞

---

### 10.5 取消点赞

**接口名称**: 取消点赞

**请求**: `DELETE /api/messages/:id/like`

**是否需要登录**: 是

**角色限制**: fan / admin / streamer

**请求参数**: 无（id 为路径参数）

**返回数据**:
```json
{
  "code": 0,
  "message": "取消点赞成功",
  "data": {
    "likeCount": 5
  }
}
```

**业务逻辑**:
1. 校验消息存在且 type = 'public'
2. **Redis 幂等检查**（同点赞，1s 内重复请求忽略）
3. 查询 likes 表确认记录存在
   - 若不存在，返回 `code: 404, message: "未点赞"`
4. 删除 likes 表记录
5. messages 表 like_count - 1
6. **写入 Redis**：设置 `like:{user_id}:{message_id} = 1`，TTL = 1秒

**特殊规则**: 
- **使用 Redis** 1s 幂等控制

---

### 10.6 举报消息

**接口名称**: 举报消息

**请求**: `POST /api/messages/:id/report`

**是否需要登录**: 是

**角色限制**: fan / admin / streamer

**请求参数**:

路径参数：id - 消息ID

Body参数：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| reason | string | 否 | 举报原因说明，≤500字符 |

**返回数据**:
```json
{
  "code": 0,
  "message": "举报提交成功，我们会尽快处理",
  "data": {
    "reportId": 50
  }
}
```

**业务逻辑**:
1. 校验消息存在
2. 校验不能举报自己的消息
3. 校验同一用户对同一消息不能重复举报（查 reports 表）
4. XSS 过滤 reason
5. 插入 reports 表（status 默认 pending）

**特殊规则**: 
- 同一用户对同一消息只能举报一次
- 消息不会被自动删除，等待管理员处理

---

## 11. 聊天室-主播回复

---

### 11.1 主播发送公开回复

**接口名称**: 主播发送公开回复

**请求**: `POST /api/messages/:id/streamer-reply`

**是否需要登录**: 是

**角色限制**: streamer

**请求参数**:

路径参数：id - 原消息ID

Body参数：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| content | string | 是 | 回复内容，1-500字符 |
| replyType | string | 是 | 回复类型：public（公开回复）/ private（私密回复） |

**返回数据**:
```json
{
  "code": 0,
  "message": "回复成功",
  "data": {
    "id": 301,
    "messageId": 2001,
    "streamerId": 90001,
    "targetUserId": 10001,
    "content": "谢谢你的支持！",
    "replyType": "public",
    "createdAt": "2026-07-23T10:05:00.000Z"
  }
}
```

**业务逻辑**:
1. 鉴权：角色必须为 streamer
2. 校验原消息存在
3. 校验 content 长度 1-500 字符 + XSS 过滤
4. **Redis 限流检查**：同消息发送冷却（20s）
5. 若 replyType = 'public'：
   - 以 streamer 身份插入 messages 表（type='public'，sender_id=streamer用户ID）
   - 在 content 前拼接标识信息或返回额外字段标识这是回复
6. 写入 Redis 冷却 key

**特殊规则**: 
- 公开回复全员可见
- 使用 Redis 消息冷却

---

### 11.2 主播发送私密回复

**接口名称**: 主播发送私密回复

**请求**: `POST /api/messages/:id/private-reply`

**是否需要登录**: 是

**角色限制**: streamer（仅主播）

**请求参数**:

路径参数：id - 原私密消息ID

Body参数：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| content | string | 是 | 回复内容，1-500字符 |

**返回数据**:
```json
{
  "code": 0,
  "message": "私密回复成功",
  "data": {
    "id": 501,
    "messageId": 2001,
    "streamerId": 90001,
    "targetUserId": 10001,
    "content": "感谢你的私信，我很感动~",
    "createdAt": "2026-07-23T10:05:00.000Z"
  }
}
```

**业务逻辑**:
1. 鉴权：角色必须为 streamer
2. 校验原消息存在且 type = 'private'
3. 校验 content 长度 1-500 字符 + XSS 过滤
4. **Redis 限流检查**：同消息发送冷却（20s）
5. 插入 private_replies 表（streamer_id=主播ID, target_user_id=原消息sender_id）
6. 写入 Redis 冷却 key
7. 写入 admin_logs（action: `create_private_reply`）

**特殊规则**: 
- **仅 streamer 可以创建**
- **仅 target_user + streamer 可见**
- admin 不可创建、不可查看

---

### 11.3 获取某消息的回复列表

**接口名称**: 获取某私密消息的回复列表

**请求**: `GET /api/messages/:id/private-replies`

**是否需要登录**: 是

**角色限制**: fan / streamer

**请求参数**: 无（id 为路径参数）

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 501,
      "messageId": 2001,
      "streamerId": 90001,
      "streamerNickname": "听潮阁小潮",
      "targetUserId": 10001,
      "content": "感谢你的私信~",
      "createdAt": "2026-07-23T10:05:00.000Z"
    }
  ]
}
```

**业务逻辑**:
1. 鉴权：角色为 fan 时，校验 target_user_id = 当前用户ID
2. 角色为 streamer 时，直接返回全部回复
3. admin 不可访问此接口
4. 查询 private_replies JOIN users 获取主播昵称
5. 按 created_at DESC 排序

**特殊规则**: 
- fan 只能看到发给自己的私密回复
- streamer 可以看到所有私密回复
- admin 无权查看

---

### 11.4 粉丝获取主播对自己的私密回复

**接口名称**: 获取主播对我的私密回复

**请求**: `GET /api/user/private-replies`

**是否需要登录**: 是

**角色限制**: fan

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码，默认1 |
| pageSize | number | 否 | 每页条数，默认20 |

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 501,
        "messageId": 2001,
        "originalMessageContent": "主播你好，想对你说...",
        "content": "感谢你的私信~",
        "createdAt": "2026-07-23T10:05:00.000Z"
      }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 1, "totalPages": 1 }
  }
}
```

**业务逻辑**:
1. 鉴权：角色为 fan
2. 查询 private_replies 表 WHERE target_user_id = 当前用户ID
3. JOIN messages 获取原消息内容
4. 按 created_at DESC 排序
5. 命中索引 `idx_target_user`

---

## 12. 个人中心-打卡

---

### 12.1 每日打卡

**接口名称**: 每日打卡签到

**请求**: `POST /api/check-in`

**是否需要登录**: 是

**角色限制**: fan

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "打卡成功",
  "data": {
    "checkDate": "2026-07-23",
    "createdAt": "2026-07-23T10:00:00.000Z",
    "consecutiveDays": 7
  }
}
```

**业务逻辑**:
1. 鉴权：角色为 fan
2. 获取当前日期（YYYY-MM-DD）
3. 查询 check_ins 表 `uk_user_date`（user_id + check_date）
   - 若已存在，返回 `{ code: 409, message: "今日已打卡" }`
4. 插入 check_ins 表
5. 计算连续打卡天数（向前追溯连续日期）

**特殊规则**: 
- user_id + date 每天只能一次（MySQL UNIQUE KEY 保障）
- 返回连续打卡天数（P1 功能）

---

### 12.2 获取当月打卡记录

**接口名称**: 获取当月打卡日历

**请求**: `GET /api/check-in`

**是否需要登录**: 是

**角色限制**: fan

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| year | number | 否 | 年份，默认当前年 |
| month | number | 否 | 月份，默认当前月 |

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "year": 2026,
    "month": 7,
    "records": [
      { "checkDate": "2026-07-01", "createdAt": "2026-07-01T08:30:00.000Z" },
      { "checkDate": "2026-07-02", "createdAt": "2026-07-02T09:15:00.000Z" },
      { "checkDate": "2026-07-23", "createdAt": "2026-07-23T10:00:00.000Z" }
    ],
    "totalDays": 3,
    "consecutiveDays": 7
  }
}
```

**业务逻辑**:
1. 鉴权：角色为 fan
2. 计算当月第一天和最后一天
3. 查询 check_ins 表 WHERE user_id = ? AND check_date BETWEEN 月初 AND 月末
4. 命中索引 `uk_user_date`（覆盖索引）
5. 计算连续打卡天数

---

## 13. 后台-聊天管理

---

### 13.1 获取全部公开消息（后台）

**接口名称**: 后台获取全部公开消息

**请求**: `GET /api/admin/messages/public`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码，默认1 |
| pageSize | number | 否 | 每页条数，默认20 |
| keyword | string | 否 | 关键词搜索消息内容 |

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "senderId": 10001,
        "senderNickname": "小潮粉丝",
        "senderUsername": "fan001",
        "senderRole": "fan",
        "content": "大家好！",
        "type": "public",
        "likeCount": 5,
        "createdAt": "2026-07-23T10:00:00.000Z"
      }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 500, "totalPages": 25 }
  }
}
```

**业务逻辑**:
1. 鉴权：admin 或 streamer
2. 查询 messages WHERE type='public'，支持 keyword LIKE 搜索
3. JOIN users 获取发送人信息
4. 分页返回

---

### 13.2 获取全部私密消息（仅 streamer）

**接口名称**: 获取全部私密消息

**请求**: `GET /api/admin/messages/private`

**是否需要登录**: 是

**角色限制**: streamer（仅主播）

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码，默认1 |
| pageSize | number | 否 | 每页条数，默认20 |
| userId | number | 否 | 按发送人ID筛选 |

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 2001,
        "senderId": 10001,
        "senderNickname": "小潮粉丝",
        "content": "主播你好...",
        "likeCount": 0,
        "createdAt": "2026-07-23T10:00:00.000Z"
      }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 50, "totalPages": 3 }
  }
}
```

**业务逻辑**:
1. **鉴权：角色必须为 streamer**，admin 返回 403
2. 查询 messages WHERE type='private'
3. JOIN users 获取发送人信息
4. 支持 userId 筛选
5. 分页返回

**特殊规则**: 
- **admin 不可查看私密消息**（返回 403）
- 仅 streamer 有权查看所有私密消息

---

### 13.3 删除消息（后台）

**接口名称**: 删除聊天消息

**请求**: `DELETE /api/admin/messages/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "消息已删除",
  "data": null
}
```

**业务逻辑**:
1. 鉴权：admin 或 streamer
2. 校验消息存在
3. 物理删除 messages 表记录（级联删除 likes、reports、private_replies）
4. 写入 admin_logs（action: `delete_message`, target_type: `message`）

---

## 14. 后台-举报工单

---

### 14.1 获取待处理工单

**接口名称**: 获取待处理举报工单

**请求**: `GET /api/admin/reports/pending`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query): page, pageSize

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 50,
        "reporterId": 10002,
        "reporterNickname": "另一个粉丝",
        "messageId": 1001,
        "messageContent": "违规内容...",
        "messageSenderId": 10003,
        "messageSenderNickname": "违规用户",
        "reason": "发布不当言论",
        "status": "pending",
        "createdAt": "2026-07-23T10:00:00.000Z"
      }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 3, "totalPages": 1 }
  }
}
```

**业务逻辑**:
1. 鉴权：admin 或 streamer
2. 查询 reports WHERE status='pending'，按 created_at DESC
3. JOIN users（举报人、消息发送人）+ messages
4. 分页返回

---

### 14.2 获取已办结工单

**接口名称**: 获取已办结举报工单

**请求**: `GET /api/admin/reports/resolved`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query): page, pageSize

**返回数据**: 同 14.1，但 status='resolved'，包含 resolvedAt 字段

---

### 14.3 查看工单详情

**接口名称**: 查看举报工单详情

**请求**: `GET /api/admin/reports/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 50,
    "reporterId": 10002,
    "reporterNickname": "另一个粉丝",
    "messageId": 1001,
    "messageContent": "违规内容...",
    "messageType": "public",
    "messageSenderId": 10003,
    "messageSenderNickname": "违规用户",
    "reason": "发布不当言论",
    "status": "pending",
    "resolvedAt": null,
    "createdAt": "2026-07-23T10:00:00.000Z"
  }
}
```

**业务逻辑**:
1. 鉴权 + 查询 reports 详情
2. JOIN 获取举报人、消息、发送人信息

---

### 14.4 标记工单为办结

**接口名称**: 标记举报工单为办结

**请求**: `PUT /api/admin/reports/:id/resolve`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "工单已办结",
  "data": null
}
```

**业务逻辑**:
1. 鉴权 + 校验工单存在且 status='pending'
2. 更新 reports 表 status='resolved'，resolved_at=NOW()
3. 写入 admin_logs（action: `resolve_report`）

---

### 14.5 删除违规消息（从工单入口）

**接口名称**: 删除被举报的违规消息

**请求**: `DELETE /api/admin/reports/:id/message`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "违规消息已删除",
  "data": null
}
```

**业务逻辑**:
1. 鉴权 + 校验工单存在
2. 通过工单找到被举报的消息
3. 物理删除 messages 表记录（级联删除 likes、reports、private_replies）
4. 自动将关联的 pending 工单标记为 resolved
5. 写入 admin_logs（action: `delete_violation_message`）

---

## 15. 后台-用户管理

---

### 15.1 获取用户列表

**接口名称**: 获取注册用户列表

**请求**: `GET /api/admin/users`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码，默认1 |
| pageSize | number | 否 | 每页条数，默认20 |
| role | string | 否 | 角色筛选：fan / admin / streamer |
| status | string | 否 | 状态筛选：active / banned |
| keyword | string | 否 | 搜索用户名或昵称 |

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 10001,
        "username": "fan001",
        "nickname": "小潮粉丝",
        "avatar": "https://cdn.example.com/avatars/1.jpg",
        "role": "fan",
        "status": "active",
        "createdAt": "2026-07-01T10:00:00.000Z"
      }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 50, "totalPages": 3 }
  }
}
```

**业务逻辑**:
1. 鉴权：admin 或 streamer
2. 查询 users 表，支持 role/status/keyword 筛选
3. JOIN avatars 获取头像 URL
4. 不返回 password_hash
5. 分页返回

---

### 15.2 拉黑用户

**接口名称**: 拉黑/封禁用户

**请求**: `PUT /api/admin/users/:id/ban`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "用户已封",
  "data": null
}
```

**业务逻辑**:
1. 鉴权 + 校验目标用户存在
2. 校验不能封禁其他 admin/streamer（仅能封禁 fan）
3. 更新 users 表 status='banned'
4. 将用户当前所有 JWT 加入黑名单（可选：通过更新密码哈希使旧 token 失效）
5. 写入 admin_logs（action: `ban_user`）

**特殊规则**: 
- 不能封禁 admin 和 streamer 角色用户
- 被封禁用户下次登录时会被拒绝

---

### 15.3 解封用户

**接口名称**: 解封用户

**请求**: `PUT /api/admin/users/:id/unban`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**:
```json
{
  "code": 0,
  "message": "用户已解封",
  "data": null
}
```

**业务逻辑**:
1. 鉴权 + 校验目标用户存在
2. 更新 users 表 status='active'
3. 写入 admin_logs（action: `unban_user`）

---

## 16. 后台-系统管理

---

### 16.1 头像池管理 - 获取列表

**接口名称**: 后台获取头像池列表

**请求**: `GET /api/admin/avatars`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query): page, pageSize

**返回数据**: 标准分页格式

---

### 16.2 头像池管理 - 新增头像

**接口名称**: 新增头像到头像池

**请求**: `POST /api/admin/avatars`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| url | string | 是 | 头像图片URL（上传后自动压缩） |
| sortOrder | number | 否 | 排序权重，默认0 |

**返回数据**:
```json
{
  "code": 0,
  "message": "头像添加成功",
  "data": { "id": 10 }
}
```

**业务逻辑**:
1. 鉴权 + 插入 avatars 表
2. 写入 admin_logs（action: `create_avatar`）

---

### 16.3 头像池管理 - 删除头像

**接口名称**: 从头像池删除头像

**请求**: `DELETE /api/admin/avatars/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**: `{ "code": 0, "message": "头像已删除", "data": null }`

**业务逻辑**:
1. 鉴权 + 删除 avatars 记录
2. 关联 users 的 avatar_id 自动置为 NULL（ON DELETE SET NULL）
3. 写入 admin_logs（action: `delete_avatar`）

---

### 16.4 敏感词管理 - 获取列表

**接口名称**: 获取敏感词列表

**请求**: `GET /api/admin/sensitive-words`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query): page, pageSize

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      { "id": 1, "word": "脏话1", "createdAt": "2026-07-01T10:00:00.000Z" }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 100, "totalPages": 5 }
  }
}
```

**业务逻辑**:
1. 鉴权 + 分页查询 sensitive_words 表

---

### 16.5 敏感词管理 - 新增敏感词

**接口名称**: 新增敏感词

**请求**: `POST /api/admin/sensitive-words`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Body):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| word | string | 是 | 敏感词内容，≤100字符 |

**返回数据**:
```json
{
  "code": 0,
  "message": "敏感词添加成功",
  "data": { "id": 101 }
}
```

**业务逻辑**:
1. 鉴权 + 校验 uk_word 唯一约束
2. 若重复返回 `{ code: 409, message: "敏感词已存在" }`
3. 插入 sensitive_words 表
4. 写入 admin_logs（action: `create_sensitive_word`）

---

### 16.6 敏感词管理 - 删除敏感词

**接口名称**: 删除敏感词

**请求**: `DELETE /api/admin/sensitive-words/:id`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数**: 无

**返回数据**: `{ "code": 0, "message": "敏感词已删除", "data": null }`

**业务逻辑**: 鉴权 + 删除 + 写日志

---

### 16.7 操作日志查询

**接口名称**: 获取操作日志列表

**请求**: `GET /api/admin/logs`

**是否需要登录**: 是

**角色限制**: admin / streamer

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码，默认1 |
| pageSize | number | 否 | 每页条数，默认20 |
| action | string | 否 | 操作类型筛选 |
| adminId | number | 否 | 操作人ID筛选 |
| targetType | string | 否 | 操作对象类型筛选 |
| startDate | string | 否 | 开始日期 |
| endDate | string | 否 | 结束日期 |

**返回数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 500,
        "adminId": 90001,
        "adminNickname": "管理员",
        "action": "delete_message",
        "targetType": "message",
        "targetId": 1001,
        "detail": "删除违规公开消息",
        "createdAt": "2026-07-23T10:30:00.000Z"
      }
    ],
    "pagination": { "page": 1, "pageSize": 20, "total": 200, "totalPages": 10 }
  }
}
```

**业务逻辑**:
1. 鉴权 + 多条件筛选查询
2. JOIN users 获取操作人昵称
3. 按 created_at DESC 排序
4. 分页返回

---

## 17. 文件上传

---

### 17.1 图片上传

**接口名称**: 上传图片文件

**请求**: `POST /api/upload/image`

**是否需要登录**: 是

**角色限制**: admin / streamer

**Content-Type**: `multipart/form-data`

**请求参数** (FormData):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | File | 是 | 图片文件，支持 jpg/png/webp/gif，最大10MB |
| category | string | 否 | 用途分类：banner/avatar/gallery/song-cover/activity-cover/character/streamer，用于存储路径 |

**返回数据**:
```json
{
  "code": 0,
  "message": "图片上传成功",
  "data": {
    "url": "https://cdn.example.com/uploads/gallery/20260723_abc123.jpg",
    "originalName": "photo.jpg",
    "size": 204800
  }
}
```

**业务逻辑**:
1. 鉴权：仅 admin / streamer 可上传
2. 校验文件类型（jpg/png/webp/gif）和大小（≤10MB）
3. 使用 `sharp` 库自动压缩：
   - 质量降至 80%
   - 最大宽度 1920px（保持比例缩放）
4. 生成唯一文件名，按 category/日期/ 存储
5. 保存到服务器或 OSS
6. 返回访问 URL

**特殊规则**: 
- 使用 sharp 自动压缩
- 仅 admin/streamer 可上传

---

### 17.2 音频上传

**接口名称**: 上传音频文件

**请求**: `POST /api/upload/audio`

**是否需要登录**: 是

**角色限制**: admin / streamer

**Content-Type**: `multipart/form-data`

**请求参数** (FormData):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | File | 是 | 音频文件，支持 mp3/wav/ogg，最大50MB |

**返回数据**:
```json
{
  "code": 0,
  "message": "音频上传成功",
  "data": {
    "url": "https://cdn.example.com/uploads/songs/20260723_abc123.mp3",
    "originalName": "song.wav",
    "size": 5242880,
    "bitrate": 192,
    "converted": false
  }
}
```

**业务逻辑**:
1. 鉴权：仅 admin / streamer 可上传
2. 校验文件类型（mp3/wav/ogg）和大小（≤50MB）
3. 检查音频码率：
   - 使用 ffprobe 或类似工具检测码率
   - 若 > 192kbps，自动转码为 192kbps MP3
4. 生成唯一文件名，按 songs/日期/ 存储
5. 返回访问 URL 及转码信息

**特殊规则**: 
- 超过 192kbps 自动转码压缩
- 仅 admin/streamer 可上传

---

## 附录 A：操作日志 action 枚举

| action 值 | 说明 |
|-----------|------|
| `create_banner` | 新增 Banner |
| `update_banner` | 编辑 Banner |
| `delete_banner` | 删除 Banner |
| `update_streamer_info` | 编辑主播资料 |
| `create_award` | 新增获奖记录 |
| `update_award` | 编辑获奖记录 |
| `delete_award` | 删除获奖记录 |
| `create_gallery_image` | 新增图集图片 |
| `update_gallery_image` | 编辑图集图片 |
| `delete_gallery_image` | 删除图集图片 |
| `create_song` | 新增歌曲 |
| `update_song` | 编辑歌曲 |
| `delete_song` | 删除歌曲 |
| `create_activity` | 新增活动 |
| `update_activity` | 编辑活动 |
| `delete_activity` | 删除活动 |
| `create_graph_character` | 新增图谱人物 |
| `update_graph_character` | 编辑图谱人物 |
| `delete_graph_character` | 删除图谱人物 |
| `create_graph_relation` | 新增关系连线 |
| `delete_graph_relation` | 删除关系连线 |
| `delete_message` | 删除聊天消息 |
| `delete_violation_message` | 删除违规消息 |
| `resolve_report` | 办结举报工单 |
| `ban_user` | 封禁用户 |
| `unban_user` | 解封用户 |
| `create_avatar` | 新增头像 |
| `delete_avatar` | 删除头像 |
| `create_sensitive_word` | 新增敏感词 |
| `delete_sensitive_word` | 删除敏感词 |
| `create_private_reply` | 主播私密回复 |

---

## 附录 B：接口总数统计

| 模块 | 接口数 |
|------|--------|
| 1. 认证模块 | 5 |
| 2. 用户模块 | 3 |
| 3. Banner | 5 |
| 4. 主播资料 | 3 |
| 5. 获奖记录 | 5 |
| 6. 图集 | 5 |
| 7. 音乐 | 5 |
| 8. 活动日历 | 5 |
| 9. 图谱 | 8 |
| 10. 聊天室 | 6 |
| 11. 主播回复 | 4 |
| 12. 打卡 | 2 |
| 13. 聊天管理 | 3 |
| 14. 举报工单 | 5 |
| 15. 用户管理 | 3 |
| 16. 系统管理 | 7 |
| 17. 文件上传 | 2 |
| **合计** | **76** |

---

## 附录 C：Redis 使用接口清单

| 接口 | Redis Key | TTL | 说明 |
|------|-----------|-----|------|
| GET /api/auth/captcha | `{captchaId}:svg_captcha` | 5min | 验证码存储 |
| POST /api/auth/logout | `jwt_blacklist:{jti}` | JWT剩余有效期 | JWT 黑名单 |
| POST /api/messages | `rate_limit:msg:{user_id}` | 20s | 消息冷却 |
| POST /api/messages/:id/like | `like:{user_id}:{message_id}` | 1s | 点赞幂等 |
| DELETE /api/messages/:id/like | `like:{user_id}:{message_id}` | 1s | 取消点赞幂等 |
| POST /api/messages/:id/streamer-reply | `rate_limit:msg:{user_id}` | 20s | 主播回复冷却 |
| POST /api/messages/:id/private-reply | `rate_limit:msg:{user_id}` | 20s | 私密回复冷却 |

---

## 附录 D：权限矩阵速查

| 接口路径前缀 | fan | admin | streamer | 游客 |
|-------------|-----|-------|----------|------|
| /api/auth/* (登录注册) | ✅ | ✅ | ✅ | ✅ |
| /api/user/* | ✅ | ✅ | ✅ | ❌ |
| /api/banners (GET) | ✅ | ✅ | ✅ | ✅ |
| /api/streamer-info (GET) | ✅ | ✅ | ✅ | ✅ |
| /api/awards (GET) | ✅ | ✅ | ✅ | ✅ |
| /api/gallery (GET) | ✅ | ✅ | ✅ | ✅ |
| /api/songs (GET) | ✅ | ✅ | ✅ | ✅ |
| /api/activities (GET) | ✅ | ✅ | ✅ | ✅ |
| /api/graph (GET) | ✅ | ✅ | ✅ | ✅ |
| /api/messages/public (GET) | ✅ | ✅ | ✅ | ✅ |
| /api/messages/private (GET) | ✅ | ❌ | ❌ | ❌ |
| /api/messages (POST) | ✅ | ✅ | ✅ | ❌ |
| /api/messages/:id/like | ✅ | ✅ | ✅ | ❌ |
| /api/messages/:id/report | ✅ | ✅ | ✅ | ❌ |
| /api/messages/:id/private-reply | ❌ | ❌ | ✅ | ❌ |
| /api/messages/:id/streamer-reply | ❌ | ❌ | ✅ | ❌ |
| /api/user/private-replies | ✅ | ❌ | ❌ | ❌ |
| /api/check-in | ✅ | ❌ | ❌ | ❌ |
| /api/admin/* | ❌ | ✅ | ✅ | ❌ |
| /api/admin/messages/private | ❌ | ❌ | ✅ | ❌ |
| /api/upload/* | ❌ | ✅ | ✅ | ❌ |

---

> 文档版本 V1.0 | 基于 PRD V2.1 + 建表SQL V1.0 | 共 76 个接口 | 2026-07-23

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
